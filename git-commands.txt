To create a new repository on the command line

	git init
	git add README.md
	git commit -m "first commit"
	git remote add origin https://github.com/mungrohiik/hello-git.git
	git push -u origin master

To push an existing repository from the command line

	git remote add origin https://github.com/mungrohiik/hello-git.git
	git push -u origin master
	
	##Problem 1 of Project Euler.net
total = 0
for i in range(2,1000):
        if(i%3==0 or i%5==0):
                total = total + i
print(total)


##Problem 2 of Project Euler.net
def compute():
	ans = 0
	x = 1  
	y = 2  
	while x <= 4000000:
		if x % 2 == 0:
			ans += x
		x, y = y, x + y
	return str(ans)
print(compute())

##Problem 3 of Project Euler.net
n=600851475143

	
